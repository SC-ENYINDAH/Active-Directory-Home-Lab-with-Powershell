# Active Directory Home with PowerShell
This project provides PowerShell scripts and guidance for managing an Active Directory (AD) home lab. It is designed to help administrators automate common AD tasks such as creating users, groups, organizational units (OUs), managing permissions and implementing account lockout policies to help protect against brute-force attacks.
